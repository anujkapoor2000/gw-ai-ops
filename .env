# Build hardening for the static bundle.
# Emit the webpack runtime as a separate file instead of an inline <script>,
# so the Content-Security-Policy can use a strict script-src 'self' with no
# 'unsafe-inline'.
INLINE_RUNTIME_CHUNK=false
# Do not ship source maps to production; they expose original source to anyone.
GENERATE_SOURCEMAP=false
